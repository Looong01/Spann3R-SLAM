# Core dependencies
numpy==1.26.4
einops
Pillow
opencv-python
tqdm
pyyaml
plyfile
natsort
scipy
packaging

# SLAM-specific
pyrealsense2
evo
torchcodec==0.1

# Spann3R / DUSt3R runtime
roma==1.5.0
huggingface-hub[torch]>=0.24.6

# Optional Spann3R tooling (demo/visualization/export)
matplotlib
trimesh
open3d
imageio[ffmpeg]
gradio==4.43.0

# Note: The following need to be installed separately:
# - PyTorch (see README.md for version matching CUDA)
# - lietorch: pip install git+https://github.com/princeton-vl/lietorch.git
# - thirdparty/in3d: pip install -e thirdparty/in3d
# - torchcodec (optional): pip install torchcodec==0.1
